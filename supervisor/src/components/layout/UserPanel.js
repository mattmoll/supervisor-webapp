import React, { Component } from "react";

export default class UserPanel extends Component {
  render() {
    return (
      <div id="user-panel">
        <h4>User settings</h4>
      </div>
    );
  }
}
